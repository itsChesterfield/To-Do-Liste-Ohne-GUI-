package liste;import org.junit.jupiter.api.Test;import static org.junit.jupiter.api.Assertions.assertEquals;import org.junit.jupiter.api.BeforeEach;public class KnotenTest {    Knoten todo;    List liste = new List();    @BeforeEach        void neuesObjekt(){            todo = new Knoten("Hello World!", 1);        }    @Test    void TestToString(){        System.out.println(todo.toString());    }    @Test    void setPriority(){        todo.setPriority(2);        System.out.println(todo);        todo.setPriority(3);        System.out.println(todo);    }    @Test    void setValue(){        todo.setValue("Hello World 2!");        System.out.println(todo.toString());        todo.setValue("Hello World 3!");        System.out.println(todo.toString());    }    @Test    void setValueLeer(){        System.out.println(todo.toString());        todo.setValue("");    }    @Test    void setValueNull(){        System.out.print(todo.toString());        todo.setValue(null);    }    @Test    void setPriorityDrüber(){        System.out.println(todo.toString());        todo.setPriority(4);    }    @Test    void setPriorityDrunter(){        System.out.println(todo.toString());        todo.setPriority(0);    }    @Test    void setPriorityMinusZahl(){        System.out.println(todo.toString());        todo.setPriority(-10);    }    @Test    void positionTest(){        liste.add("Test", 1);        liste.add("Test2", 2);        liste.add("Test3", 3);        liste.ausgabe();    }    @Test    void switchtigStandard(){        List testi = new List();        Knoten k = new Knoten("Hello World", 1);        Knoten k2 = new Knoten("TestRaum", 3);        testi.add(k);        testi.add(k2);        testi.ausgabe();        testi.swichting(k, k2);        testi.ausgabe();    }Test}